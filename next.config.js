/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upcdn.io",
      "pbxt.replicate.delivery",
      "pbxt.replicate.delivery",
      "replicate.delivery",
    ],
  },
};

module.exports = nextConfig
